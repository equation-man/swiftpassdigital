// next.config.js
/* @type {import('next').NextConfig} */
const nextConfig = {
  output: "standalone",
  
  images: {
	  unoptimized: true,
  },

  compress: false,
};

module.exports = nextConfig;
































